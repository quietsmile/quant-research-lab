# 信号严谨性与收益：能挣多少、能挣到吗、为什么别人更高

本文记录对 ML 价量信号「可落地性」与「收益归因」的系统调查。结论一句话：**信号的横截面区分度真实，但扣掉 beta/成本/泄漏后，可落地的独立 alpha 很弱且低容量；别人 30–70% 的年化大头是泄漏 + 小盘 beta，而非真本事。**

所有脚本在 `examples/`，回测口径：liq1500 大中盘、含退市、2021–2026 OOS、walk-forward + 20 日 purge、扣双边成本。

---

## A. 乐观度拆解：30–70% 年化从哪来（`optimism_budget.py`）

同一个生产信号，逐步松绑严格项，看年化如何虚高：

| 松绑步骤 | 年化 | 夏普 | 回撤 |
|---|---|---|---|
| 0 严格基线（OOS/大中盘/真实撮合/扣成本/T+1开盘/hold10） | **+10%** | 0.72 | -31% |
| 1 +理想撮合（去涨跌停/停牌/ST） | +10% | 0.69 | -31% |
| 2 +去成本（毛收益） | +14% | 0.99 | -27% |
| 3 +日频调仓（hold=1） | +16% | 0.99 | -31% |
| 4 +下沉小盘（liq1500 内小市值半区） | +19% | 0.92 | -30% |
| 5 **+样本内信号（全期训练，泄漏）** | **+51%** | **1.82** | -30% |

**关键发现**：
- 去成本/日频/小盘（无泄漏的乐观/beta 选择）：+10% → +19%（×1.9）。
- **只加一步「样本内训练」（去掉 walk-forward）：+19% → +51%、夏普 0.92→1.82。** 同一信号、同一特征、同一模型，唯一区别是少了 walk-forward。
- 别人 30–70% 的年化，落点几乎精确命中「样本内泄漏」档（+51%）。

**结论**：那 30–70% 的大头是**样本内泄漏**，其次是小盘 beta + 无成本 + 日频。我们 +10% 的「低」，主要是严格的结果，不是模型差。真实可争取的合法空间在步骤 2–4 那 +10%→+19% 里的一部分（成本/持有期/特征/模型），但大中盘 + 扣成本 + 无泄漏下干净上 50% 物理上做不到。

---

## B. 真实杠杆：特征扩展 / 训练窗口 / 调参

### B-1 特征扩展：25 子集 vs Alpha158 扩展（`feature_expansion.py`）

季度代理、多视野 rank 口径：

| 特征集 | RankIC | ICIR | 胜率 | #因子 |
|---|---|---|---|---|
| 25 因子子集（现状，含基本面） | **0.0881** | **0.531** | 70% | 25 |
| Alpha158 扩展（纯量价 85） | 0.0705 | 0.452 | 67% | 85 |
| 两者合并 | 0.0892 | 0.550 | 71% | 110 |

**结论：特征不是真实杠杆。** 机械扩到 85 个 Alpha158 量价因子**反而更差**（缺基本面 + 噪声/冗余）；合并仅 +0.001 RankIC（噪声级）。我们 25 个（精选量价 + 基本面）已抓住主要信息，瓶颈是 SNR 不是特征数。

### B-2 训练窗口：扩张窗 vs 滚动 48/36/24 月（`window_compare.py`）

| 训练窗口 | RankIC | ICstd | ICIR | t值 |
|---|---|---|---|---|
| 扩张窗（现状） | 0.0881 | 0.166 | 0.531 | 19.2 |
| 滚动 48 月 | 0.0889 | 0.165 | 0.538 | 19.5 |
| 滚动 36 月 | 0.0872 | 0.163 | 0.534 | 19.3 |
| **滚动 24 月** | **0.0909** | **0.158** | **0.576** | **20.8** |

**结论：滚动 24 月 > 扩张窗**，RankIC +0.003、ICIR +8.5%、IC 更稳。A 股非平稳下"近 2 年"比"全历史"更跟得上风格。

**已采纳并全口径确认**（`train_signal(lookback_m=24)`，重训生产信号）：

| 指标 | 旧（扩张窗） | 新（滚动24月） |
|---|---|---|
| 全口径 mean IC | 0.0837 | **0.0853** |
| 持有10日 真实撮合 夏普 | 0.72 | **0.79** |
| 持有10日 年化 / Calmar | +10% / 0.33 | **+11% / 0.38–0.41** |
| N20 对冲后净 alpha 夏普（中证500，滚动β扣贴水） | +0.25 | **+0.32**（毛 +0.84→+0.92） |
| 2024Q1（小盘危机试金石） | +5.1% / 回撤-4.2% | **+8.4% / 回撤-3.0%** |
| 2022 全年 | -14% | **-10%** |

**这次 IC 提升真实传导到了策略、对冲后 alpha、甚至 2024Q1 的鲁棒性**（不同于 rank 标签那次只提 IC 不提策略）——是全程唯一既提 IC 又提可落地指标的改进。已落地为生产默认（`train_signal(lookback_m=24)`，`lookback_m=None` 可退回扩张窗）。大局不变（仍集中 Top-20、净 alpha 薄、低容量），但这是干净的真实增量。

### B-3 超参搜索

> 早先 IC 实验已测：更深(leaves63)/强正则在 rank-label 基座上几乎不动（0.083 附近），低杠杆。

---

## C. 容量/参与率（`capacity_curve.py`）

给 Top-20 策略加平方根冲击模型（额外冲击只对 >0.5%ADV 的单子计，避免与 base 成本双重计费）：

| AUM | order/ADV | >10%ADV占比 | 年化冲击拖累 | 净年化 |
|---|---|---|---|---|
| 1000万 | 0.002 | 0% | 1.1% | +9% |
| 3000万 | 0.005 | 0% | 5.3% | +5% |
| ~5000万–1亿 | 0.018 | 4% | ~15% | **≈0（天花板）** |
| 3亿 | 0.055 | 27% | 29% | -19% |
| 10亿 | 0.183 | 69% | 56% | -46% |

**结论：Top-20 资金容量天花板 ≈ 5000万–1亿**，超过被冲击吃光 +10%（对冲后净 alpha 更薄、容量更小）。edge 集中头部 → 既难分散、又装不下钱，**是个小资金策略**。补上了 participation/容量维度（对照 ashare-lowfreq-research 回测引擎）。

## D. 时序模型（GRU/TFT）融合（`ml_gru_fusion.py`）

**实测受阻（环境）**：本机 torch 2.8（按 numpy 1.x 编译）与 numpy 2.4.6 ABI 不兼容，numpy↔tensor 转换报 "Numpy is not available"，喂数据无法进行；降级 numpy 会牵连 lightgbm/pandas/整条已验证管线，不值得。脚本已就绪，待修复 torch/numpy 版本后可直接跑。

**基于先验的结论（暂不投入 TFT）**：
- 行业实证：日频截面上 GBDT ≈ 或 > 深度时序（Qlib Alpha158/360 benchmark），深度模型在高频/文本才拉开差距。
- 我们的特征已编码多窗口时序（ret/ma/std/...），序列模型可挖的增量小。
- 本轮一连串负结果（特征扩展 null、另类因子 null、breadth 不救）都指向：瓶颈是 SNR、不是模型类别。再加一个 TFT 大概率同样故事。
- 正确做法是先用轻量 GRU 测「与 LGBM 的去相关性 + 融合增量」，去相关且有增量才上 TFT——但据上述先验，预期增量很小。

---

## 总结：为什么我们年化只有十几，别人 30–70%

| 来源 | 性质 | 我们 |
|---|---|---|
| 样本内泄漏（去 walk-forward） | **虚高**（+19%→+51%，最大单一来源） | 控住 |
| 小盘 beta + 无成本 + 日频 | beta/乐观 | 控住 |
| 特征数 | **非杠杆**（扩 Alpha158 反而更差） | 25 个已够 |
| 模型类别（深度/时序） | **非杠杆**（SNR 瓶颈） | 暂不投入 |
| 训练窗口 | **真实小杠杆**：滚动 24 月 ICIR +8.5% | 可采纳 |
| 资金容量 | Top-20 天花板 ~5000万-1亿 | 小资金 |

**一句话**：我们 +10% 的「低」主要是严格（无泄漏、扣成本、大中盘、含退市）的结果，不是模型差；别人 30–70% 大头是泄漏 + 小盘 beta。真实可争取的合法空间很窄（滚动窗 +小、调参≈0、特征/模型 null），且这点 alpha 弱（对冲后净 +0.25 夏普）又低容量。**结论与全程一致：价量 ML 信号的井已挖到底，可落地价值有限，L5（大盘价值+低波）仍是更稳的选择。**

## 已确立的方法论红线（前序调查，详见飞书子文档）

- **对冲归因**：标准做法 = 滚动 β 中性（PIT，禁用全样本 β = 未来函数）；更严谨 = Barra 多因子中性。滚动 β 中性后 N20 对中证500 残差 alpha 毛夏普 +0.84，扣 IC 贴水后仅 +0.25、N≥50 转负 → 半中证500 beta + 半弱 alpha。
- **多空 dollar-neutral 纸面夏普 ≠ 收益**（A 股个股不可做空），只作信号体检。
- **分层单调性**：top1% +1.90%/10日最强、无 roll-off → Top-20 低夏普是 breadth 问题；但可落地 P&L 显示扩 breadth 不救（edge 集中头部、衰减快过 √breadth）。
- **IC 提升技巧**：标签横截面 rank（+50%）、多视野集成（+6%）；样本外 RankIC 0.0477→0.0837，ICIR 0.515。
- **泄漏审计**：随机切分/label 时点/未来函数/purge 全干净。
